<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>Player Profile</title>
        <link rel="stylesheet" href="/style.css" type="text/css">
    </head>
    <body>
        <h1>Welcome <%=player.name%></h1>

        <form action="/players/<%=player.id%>?_method=PUT" method="POST"> <!-- changed from 'index' to 'player.id' -->
            Name: <input type="text" name="name" value=<%=player.name%> /><br> <br>
            Username: <input type="text" name="username" value=<%=player.username%> /> <br> <br>
            Password: <input type="text" name="password" value=<%=player.password%> /><br> <br>
            Team: <select name = "teamId">
                <% for (let i = 0; i < teams.length; i++) { %>
                <% if (teams[i].id === player.teamId) { %>
                    <option value = "<%=teams[i].id%>" selected ="<%=teams[i].id%>"><%=teams[i].name%></option>
                <% } else{ %>
                    <option value = "<%=teams[i].id%>"><%=teams[i].name%></option>
                <% } %>    
                <% } %>
            </select><br /><br />
            
            Add Pokemon: <select name = "pokemon">
                <% for (let i = 0; i < pokes.length; i++) { %>
                <option value = "<%= pokes[i].id%>"><%=pokes[i].name%></option>
                <% } %>
            </select>
            <br /><br />
            <input type="submit" value="Edit Profile" />
        </form>

        <br>
        <form action="/players/<%=player.id%>?_method=DELETE" method="POST">
            <input type="submit" value="Delete User" />
        </form>
        <br /><br />
        Player has added:
        <% for (let i = 0; i<player.Pokemons.length; i++) { %>
            <li>
                <%= player.Pokemons[i].name %>
            </li>
            <br />
        <% } %>  
        <br /><br />

    </body>
</html>